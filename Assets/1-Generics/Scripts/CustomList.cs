using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*namespace Generics
{
    public class CustomList<IceT> 
    {
        private IceT[] data;
        public int amount;

        public CustomList() { amount = 0; }

        public void Add(IceT item)
        {
            //Create new array of amount + 1
            IceT[] cache = new IceT[amount + 1];
            //check all existing items to new array
            for(int i = 0; i < data.Length; i++)
            {
                //copy each element to cache
                cache[i] = data[i];
            }
        }
       
    }
}*/
